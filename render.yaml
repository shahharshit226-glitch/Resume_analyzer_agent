services:
  - type: web
    name: resumeanalyser-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main_with_email_agent:app --host 0.0.0.0 --port $PORT
